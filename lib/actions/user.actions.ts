'use server';

export const signIn = async () => {
  try {

  } catch (error) {
    console.error(error);

  }
}

export const signUp = async (useData: SignUpParams) => {
  try {

  } catch (error) {
    console.error(error);

  }
}